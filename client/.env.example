# Better Auth client configuration
# Local defaults point to the Better Auth dev server; override with production
# values (e.g., https://auth.onemainarmy.com/api/auth) during deployment.
VITE_AUTH_BASE_URL=http://localhost:25000/api/auth
VITE_MCP_METADATA_URL=http://localhost:25000/.well-known/oauth-protected-resource
VITE_TODO_API_BASE_URL=http://localhost:3000

# Optional: override Vite base path when building for production
VITE_CLIENT_BASE=/
# Optional: disable the Better Auth gate in emergencies (set to "false")
VITE_ENABLE_AUTH_GATE=true
